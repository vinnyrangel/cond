<!DOCTYPE html>
<html lang="en">
	<head>
	  <meta charset="utf-8">
	  <title>Condominio</title>
	  <%= csrf_meta_tags %>

	  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
	  <!--[if lt IE 9]>
	    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
	  <![endif]-->
	  <%= javascript_include_tag "application" %>
	  <%= stylesheet_link_tag "application", :media => "all" %>
	</head>
  <body>

    <%= development_info %>

    <div class="container-fluid">
      <div class="top">
        <div class="ltop">
          <span>eumoro.no</span>
          <span class="condominio"><%= link_to meu_condominio.nome, meu_condominio %></span>
        </div>

        <div class="rtop">
          Olá <%= current_usuario.nome %>, você tem <span>3 mensagens novas</span>.
        </div>
      </div>

      <div class="logo">
        <!-- %= #image_tag meu_condominio.logo.url %-->
        <%= link_to ( image_tag meu_condominio.logo.url ), meu_condominio %>
      </div>

    <div class="row-fluid">
      <div class="span2">
        <!--Sidebar content-->

        <%= link_to "Minha Home" %>

        <ul class="nav nav-list">
          <li class="nav-header">Perfil</li>
          <li><%= link_to "Meus Dados" %></li>
          <li><%= link_to "Configurações" %></li>
        </ul>

        <ul class="nav nav-list">
          <li class="nav-header">Condominio</li>
          <li><%= link_to "Avisos", condominio_avisos_path(:condominio_id => meu_condominio) %></li>
          <li><%= link_to "Reservas" %></li>
          <li><%= link_to "Classificados" %></li>
          <li><%= link_to "Chamados" %></li>
		  <li><%= link_to "Serviços" %></li>
        </ul>


        <ul class="nav nav-list">
          <li class="nav-header">Páginas</li>
          <%= link_to "Páginas", paginas_path %> <%= link_to "", new_pagina_path, :class => "icon-edit" if can? :create, Pagina %>
          <% if paginas_publicas and not paginas_publicas.all.empty? %>
          <% paginas_publicas.each do |pagina| %>
          <li>
            <a href="<%= url_for(pagina) %>">
              <i class="icon-file"></i>
              <%= pagina.titulo %>
            </a>
          </li>
          <% end %>
        </ul>
        <% end %>
      </div>
      <div class="span10">
        <!--Body content-->
      <%= yield %>
      </div>
    </div>
  </div>

  </body>
</html>
